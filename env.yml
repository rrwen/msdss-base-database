# run: conda env create -f env.yml
name: msdss-base-database
channels:
  - conda-forge
dependencies:
  - python=3.8
  - pip
  - pip:
    - jupyter_sphinx
    - sphinx-material
    - sphinx-copybutton
  - build
  - psycopg2
  - sphinx
  - twine